#include<stdio.h>
int main()
{
	char letters[50];
    int glass[2][2]={1,0,0,0},n;
    scanf("%d",&n);
    fflush(stdin);
    fgets(letters,sizeof letters,stdin);
	while(letters[n]!='\0')
	{
		switch (letters[n])
		{
		case 'A':;
		case 'B':;
		case 'C':;
		case 'D':;
	    }
		n++;
	}
	return 0;
}
